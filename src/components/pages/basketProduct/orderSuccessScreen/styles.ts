import styled from "styled-components";
import { View, Text, Image } from "react-native";

export const MainContainer = styled(View)`
  width: 100%;
  display: flex;
  align-items: center;
`;

export const TextMessage = styled(Text)`
  text-align: center;
  padding: 5px;
  width: 85%;
  font-size: 20px;
`;

export const SuccessPicture = styled(Image)``;
